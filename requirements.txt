# 通用 RAG 知识库系统 后端依赖
# Python 3.12（开发使用 3.12.13）
# 安装： python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
#
# 版本区间尽量贴合当前 .venv 实测环境，减少复现差异。

# Web 服务
fastapi>=0.115,<2
uvicorn>=0.30
pydantic>=2,<3

# 向量库
chromadb>=0.5

# embedding 模型
sentence-transformers>=3,<6
torch>=2,<3

# LLM 客户端（DeepSeek 走 OpenAI 兼容协议）
openai>=1.50

# 文档解析
python-docx>=1.1
pypdf>=4

# 测试（当前 .venv 尚未安装，做测试阶段前需补装）
pytest>=8
httpx>=0.27
